<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>{{title}}</title>
  <style>
    * {
      box-sizing: border-box;
      font-family: 'Arial', sans-serif;
    }

    body {
      margin: 0;
      padding: 0;
      background: #fff;
    }

    .container {
      display: flex;
      flex-direction: row;
      width: 100%;
      height: 100vh;
      overflow: hidden;
    }

    .col {
      flex: 1;
      padding: 30px;
    }

    .left {
      background-color: #f2f2f2;
    }

    .center {
      background-color: #d84315;
      color: white;
    }

    .right {
      background-color: #fff;
      border-left: 3px solid #d84315;
    }

    h1, h2 {
      margin-top: 0;
    }

    .title {
      font-size: 22px;
      font-weight: bold;
      color: #d84315;
    }

    .logo {
      text-align: center;
      margin-bottom: 20px;
    }

    .logo img {
      max-width: 100px;
    }

    .contact {
      margin-top: 30px;
      font-size: 14px;
    }

    .highlight {
      font-weight: bold;
      color: #d84315;
    }

    img {
      max-width: 100%;
      margin-top: 20px;
      border-radius: 5px;
    }

    footer {
      margin-top: 30px;
      font-size: 12px;
      color: gray;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="col left">
      <h2 class="title">¿QUIÉNES SOMOS?</h2>
      <p>{{about}}</p>
      {{#if teamImage}}
        <img src="{{teamImage}}" alt="Equipo de trabajo" />
        <p><em>Equipo de trabajo de la constructora.</em></p>
      {{/if}}
    </div>

    <div class="col center">
      <div class="logo">
        {{#if logoUrl}}<img src="{{logoUrl}}" alt="Logo" />{{/if}}
      </div>
      <p>{{mission}}</p>

      <div class="contact">
        <h3>CONTÁCTANOS</h3>
        <p>📞 {{phone}}</p>
        <p>📧 {{email}}</p>
        <p>📍 {{address}}</p>
        <p>🌐 {{website}}</p>
      </div>
    </div>

    <div class="col right">
      <h2 class="title">{{ctaTitle}}</h2>
      <p class="highlight">{{ctaHighlight}}</p>
      {{#if bannerImage}}
        <img src="{{bannerImage}}" alt="Edificio o proyecto" />
      {{/if}}

      <footer>
        Generado con MagicDesign AI
      </footer>
    </div>
  </div>
</body>
</html>
